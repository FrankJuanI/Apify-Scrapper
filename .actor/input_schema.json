{
    "title": "LinkedIn Company Jobs Scraper",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
      "companyIds": {
        "title": "LinkedIn Company IDs",
        "type": "array",
        "description": "Lista de IDs de empresas (números) que deseas scrapear.",
        "items": {
          "type": "string"
        }
    }
    },
    "required": ["companyIds"]
}